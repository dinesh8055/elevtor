import React from 'react';

const Search = React.createClass({
  render() {
    return (
      <div>

          Search Page (no user sign in)
          OnClick: Check either JT or L contain value

Yes: Redirect to search result page

No: Display error message
        
      </div>
    )
  }
});

export default Search;
