import React from 'react';

const CreateResume = React.createClass({
  render() {
    return (
      <div>

          Create Resume Page
          OnClick: Check user signed in


Yes: redirect to resume recording page

No: redirect to signup page
      
      </div>
    )
  }
});

export default CreateResume;
