import React from 'react';

const SearchForExperts = React.createClass({
  render() {
    return (
      <div>
        Search for Experts


OnClick: Check user signed in


Yes: redirect to profile search page

No: redirect to signup page
      </div>
    )
  }
});

export default SearchForExperts;
